# コマンド

### dnfの一覧を取得、検索
```--installed```インストールされているものだけを表示
```linux
dnf list --installed | grep -i <検索したいパッケージ名>
```
### dnfのパッケージ最新バージョン確認
現在検索対象のパッケージがどのバージョンまでリリースされているか確認
```linux
dnf check-update | grep -i <検索したいパッケージ名>
```

### dnfパッケージのアップデート
スーパーユーザー権限で実行する。  
```linux
sudo dnf upgrade <パッケージ名>
```